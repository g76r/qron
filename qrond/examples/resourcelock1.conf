#(pf (version 1.0))
(config
 (taskgroup app
   (param foobar '%{=env:FOOBAR_%{!tasklocalid}:FOOBAR:foobar}')
   (param foo '%{=env:FOO}')
   (param bar '%{=env:BAR:bar}')
 )
 (task t1
  (taskgroup app)
  (mean local)
  (command 'sleep 60 # foobar=%foobar foo=%foo bar=%bar FOOBAR=%{=env:FOOBAR}' )
  (trigger (cron * * * * * *))
  (resource pencil 1)
  (maxinstances 10)
 )
 (task t2
  (taskgroup app)
  (mean local)
  (command 'sleep 60 # foobar=%foobar foo=%foo bar=%bar')
  (trigger (cron * * * * * *))
  (resource pencil 1)
  (maxinstances 10)
 )
 (host localhost(hostname localhost)(resource pencil 4))
 (alerts
   (gridboard tasks
    (label Tasks Alerts)
    (pattern '^task\.(?<status>[^\.]+)\.(?<taskid>.+)$')
    (dimension taskid)
    (dimension status)
    (param gridboard.rowformat '<a href="../tasks/%1">%1</a>')
   )
 )
 (log(level debug)(file "/tmp/qron-debug-%{=date:yyyyMMdd}.log")(unbuffered))
 (maxtotaltaskinstances 32)
)
